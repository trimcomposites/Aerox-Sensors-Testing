import 'package:flutter/material.dart';

class ErrorDetailsScreen extends StatelessWidget {
  const ErrorDetailsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('ERROR!!'),
    );
  }
}