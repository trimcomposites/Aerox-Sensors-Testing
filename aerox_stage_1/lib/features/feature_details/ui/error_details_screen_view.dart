import 'package:flutter/material.dart';

class LoadingDetailsScreenView extends StatelessWidget {
  const LoadingDetailsScreenView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('ERROR!!'),
    );
  }
}