import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('mock racket datasource ...', (tester) async {
    group('remote get rackets', (){
      test('success get rackets, must return [ List<Racket> ]', ()async{

      });
    });
  });
}